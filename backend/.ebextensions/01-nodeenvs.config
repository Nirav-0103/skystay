option_settings:
  # Nginx reverse proxy: forward port 80 → app process
  aws:elasticbeanstalk:environment:proxy:
    ProxyServer: nginx

  # Default environment
  aws:elasticbeanstalk:application:environment:
    NODE_ENV: production
